# Consumer-seed gate — 2026-08-06 (first run, gate introduction)

Verbatim gate output below; three notes first, recorded the day the gate was born so its first
green is readable honestly.

1. **Attempt 1 of `capabilities-crossover` settled incomplete** with
   `settlement.incomplete.engine_error` after a ~6s engine run — a transient engine failure, not
   a miss. Attempt 2 completed and carried the finding, line-anchored. This is precisely the
   serving-variance class the `--attempts 2` / any-attempt-found policy exists for (the same
   policy the corpus qualification's isolated-opinion re-runs apply), demonstrated on the gate's
   very first run.
2. **The evidence's spend total undercounts.** The incomplete attempt reports `spend 0` in the
   CLI's JSON report while its `model.usage` diagnostic recorded requests 4, prompt 14,955,
   completion 886 — so the true total for this run is roughly 113k tokens, not the 97,233 the
   table carries. Flagged as its own follow-up against `src/review.ts` (an incomplete run that
   burned tokens must not report zero), not repaired here.
3. **v0.19.1's proxy healing fired on every attempt.** All three model-backed runs show
   `cache_key_rejected: 1` with `bad_request_persisted: 0`: the endpoint 400s the first
   cache-keyed request, the keyless retry (KfQ#150, stage 1) heals it, nothing persists as
   failed. First live confirmation of the v0.19.1 heal path outside CI — and consistent with the
   hypothesis that Keiko#3008's CI 400s are endpoint-configuration, not content.

Both seeds were planted on `origin/dev @ 77b01662` (post-#3008) in
`packages/keiko-server/src/qualityIntelligence/connectorAuthorization.ts`; noise was zero on
every attempt — no findings outside the seeded file.

---

# Consumer-seed gate — 2026-08-06

- Reviewer under test: keiko-for-quality 0.19.1
- Model: gpt-oss-120b (openai)
- Base: origin/dev @ 77b01662af08
- Verdict: GREEN (required failures: none)
- Total spend (tokens): 97233

## auth-default-flip (tier 1, required)

- Status: passed
- Attempt 1: outcome complete, 1 finding(s) in the seeded file (line-anchored), noise 0, spend 35886

## capabilities-crossover (tier 2, required)

- Status: passed
- Attempt 1: outcome incomplete, 0 finding(s) in the seeded file, noise 0, spend 0
- Attempt 2: outcome complete, 1 finding(s) in the seeded file (line-anchored), noise 0, spend 61347
